<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GrinPlusPlus.Views.SendingGrinsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    xmlns:resource="clr-namespace:GrinPlusPlus.Resources"
    xmlns:sharedTransitions="clr-namespace:Plugin.SharedTransitions;assembly=Plugin.SharedTransitions"
    prism:ViewModelLocator.AutowireViewModel="True"
    BackgroundColor="Black">
    <StackLayout
        HorizontalOptions="Center"
        Spacing="40"
        VerticalOptions="Center">
        <Label
            FontSize="20"
            HorizontalTextAlignment="Center"
            Text="{x:Static resource:AppResources.TransactionSent}"
            TextColor="White"
            VerticalTextAlignment="Center" />
        <Button
            BackgroundColor="#191919"
            Command="{prism:NavigateTo '/SharedTransitionNavigationPage/DashboardCarouselPage',
                                       UseModalNavigation=False,
                                       Animated=True}"
            CornerRadius="5"
            HeightRequest="44"
            Text="{x:Static resource:AppResources.Close}"
            TextColor="#ffff00" />
    </StackLayout>
</ContentPage>